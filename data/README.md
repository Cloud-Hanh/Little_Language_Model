# Data Policy

This repository does not commit:

- large raw corpora
- generated dictionary outputs
- temporary packed datasets

Keep large files local, for example in:

- `data/raw/`
- `data/processed/`
- `artifacts/`

These paths are ignored by git.
